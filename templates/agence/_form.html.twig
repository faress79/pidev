{{ form_start(form, {'attr': {novalidate: 'novalidate'}}) }}

{# Affichage des erreurs globales du formulaire #}
{{ form_errors(form) }}

<div class="col-sm-10">
	<label for="nom">Nom</label>
	{{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
	{{ form_errors(form.nom,{'class':'text-danger'}) }}
</div>


<div class="form-group">
	<label for="tel">Téléphone</label>
	{{ form_widget(form.tel, {'attr': {'class': 'form-control'}}) }}
	{# Affichage des erreurs pour le champ tel #}
	{{ form_errors(form.tel) }}
	{{ form_errors(form.tel,{'class':'text-danger'}) }}

</div>

<div class="form-group">
	<label for="address">Adresse</label>
	{{ form_widget(form.address, {'attr': {'class': 'form-control'}}) }}
	{# Affichage des erreurs pour le champ address #}
	{{ form_errors(form.address) }}
	{{ form_errors(form.address,{'class':'text-danger'}) }}

</div>

<div class="form-group">
	<label for="email">Email</label>
	{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
	{# Affichage des erreurs pour le champ email #}
	{{ form_errors(form.email) }}
		{{ form_errors(form.email,{'class':'text-danger'}) }}

</div>

<div class="form-group">
	<label for="image">Image</label>
	{{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
	{# Affichage des erreurs pour le champ image #}
	{{ form_errors(form.image) }}
	{{ form_errors(form.image,{'class':'text-danger'}) }}

</div>

<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
